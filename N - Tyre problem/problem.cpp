#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int N, M;
        cin >> N >> M;

        int total = (2 * N) + (4 * M);
        cout << total << endl;
    }

    return 0;
}