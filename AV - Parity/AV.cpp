#include <bits/stdc++.h>
using namespace std;

int main (){
          int T,N;
          cin >> T;
          while(T--){
                    cin >> N;
                    if (N % 2 == 0) cout << "Yes\n";
                    else cout << "No\n";
          }
          return 0;
}